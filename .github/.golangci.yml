version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gocritic

issues:
  exclude-rules:
    - path: ".*_test\\.go"
      linters:
        - errcheck
        - gocritic
